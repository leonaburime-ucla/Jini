# Bug Hunt

This sample contains one intentional defect in a tiny shopping-cart calculation. It uses
Node's built-in test runner and has no dependencies.

```sh
npm test
```

Ask Jini to run the tests, explain the failure, and make the smallest correct fix. The
expected total is the subtotal after per-line discounts, plus tax.
