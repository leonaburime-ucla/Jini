# Memory

Toolkit-owned memory system definitions and architecture notes.

This folder does not hold live project memory. Runtime memory files live in the workspace surface at `<ADS_MEMORY_ROOT>/knowledge/`, with the repo-local template copy under `project-knowledge-template/knowledge/`.
