# OD PR Inventory Coverage Audit

Audit of `leonaburime-ucla/open-design` (fork of `nexu-io/open-design`) branches, all authored by
leonaburime-ucla, all either closed-without-merge or still-open PRs against `nexu-io/open-design`.

For each branch: checked out in `/tmp/od-audit`, located the real test suite for the package(s) it
touches, ran it, and captured pass/fail counts and coverage (statements/branches/functions/lines) if a
coverage script/config exists. Also checked mergeability against the fork's current `main` via
`git merge-base` + a dry-run merge into a throwaway branch (never merged into anything real).

Working notes: this file is checkpointed — one row appended (commit + push) immediately after each
individual branch finishes, not batched. If this run stops early, everything below the header is real,
verified progress; anything not yet listed has simply not been audited yet.

Status legend: ✅ complete audit · ⚠️ partial/blocked (see note) · ❌ branch not found

## Results

| Priority | Branch | PR# | Status | Test suite found | Pass/Fail | Coverage (stmts/branch/func/lines) | Mergeable vs main | Note |
|---|---|---|---|---|---|---|---|---|
| 3 | codex-capability-barrel | #5150 | ✅ | Yes — `apps/daemon` vitest (`pnpm test` in `apps/daemon`, `vitest.config.ts`) | Files: 415 passed / 9 failed / 4 skipped (428). Tests: 5413 passed / 25 failed / 7 skipped / 4 todo (5449) | No coverage script/config found for `apps/daemon` (`vitest run --coverage` errors: `@vitest/coverage-v8` not installed; no coverage script in `apps/daemon/package.json` or root `package.json`) | Yes — `git merge-base` origin/main == origin/main HEAD (0b88ef561) for this branch, dry-run `git merge --no-commit --no-ff` clean, 0 conflicts | All 25 failing tests are outside the codex domain the branch touches (amr-session-resume, connection-test auth/CLI-spawn, media/policy-routes, xai/tokens EACCES-as-root) — same failure signatures reproduced independently on `memory-capability-barrel`'s run, strongly suggesting these are pre-existing/environment-driven (sandboxed process spawning, no real network/CLI auth, tests run as root so EACCES simulation via chmod 0o000 doesn't hold) rather than caused by this branch. No codex-specific test failed. |
| 1 | memory-capability-barrel | #5131 (closed) | ✅ | Yes — `apps/daemon` vitest (`pnpm test` in `apps/daemon`, `vitest.config.ts`) | Files: 418 passed / 8 failed / 4 skipped (430). Tests: 5418 passed / 24 failed / 7 skipped / 4 todo (5453). Regression test `tests/plugin-cli-dynamic-imports.test.ts` (added by this branch's tip fix commit) independently re-run and confirmed passing (3/3). | No coverage script/config found for `apps/daemon` (same as codex-capability-barrel — `@vitest/coverage-v8` not installed, no coverage script anywhere in the package) | Yes — merge-base == origin/main HEAD (0b88ef561), dry-run merge clean, 0 conflicts | **PR#5131's known bug (stale `./plugins/...` dynamic-import path after the CLI subcommand move) IS fixed and IS test-covered.** Tip commit `5d9cf7267` "repoint plugin subcommand dynamic imports after cli split" adds `tests/plugin-cli-dynamic-imports.test.ts`, which resolves every relative dynamic-import specifier in `src/cli/plugin/*` against disk and also actually executes the scaffold import path at runtime (guards `ERR_MODULE_NOT_FOUND`, not just a static check). The 24 failing tests are the exact same failure set seen on codex-capability-barrel (amr-session-resume, connection-test, media/policy-routes, xai/tokens EACCES-as-root, run-failure-telemetry-smoke) minus one (`run-retry-runtime`) — strong independent corroboration these are environment-driven baseline failures, not branch-caused. IMPORTANT CAVEAT: the first attempt to run this suite produced 125 failed files / 78 failed tests, almost entirely `Failed to resolve entry for package "@open-design/sidecar-proto"` — the root `postinstall` build lifecycle was silently aborted mid-sequence by `electron`'s own postinstall trying to download a binary (`HTTPError: Response code 403`, blocked by this sandbox's network policy) and never got to building `packages/sidecar-proto`/`apps/daemon`. Fixed by re-running `pnpm install` with `ELECTRON_SKIP_BINARY_DOWNLOAD=1`, which lets the rest of `scripts/postinstall.mjs`'s build sequence complete; this env var is now used for every subsequent branch's install in this audit. Anyone re-running this audit (or CI in a similarly network-restricted sandbox) should set it too, or the whole daemon suite reports mass false failures that look like nothing works. |
| 1 | cli-capability-barrels (successor/predecessor of PR #5127's branch — exact original branch name not found, see note) | #5127 (closed, superseded) | ⚠️ (light audit, not full independent test run — see note) | N/A — not independently run | N/A | N/A | Yes — merge-base == origin/main HEAD, dry-run merge clean, 0 conflicts (checked via batch script) | PR #5127's exact branch name no longer exists on `git branch -a`, matching the task's expectation that it may have been superseded early. Found `cli-capability-barrels` instead, which is cli-domain-shaped and whose tip commit (`9483293`, "repoint plugin subcommand dynamic imports after cli split") has the **identical commit message** to memory-capability-barrel's tip fix (`5d9cf726`, same message, 26 min later). `git diff origin/cli-capability-barrels origin/memory-capability-barrel --stat` confirms `cli-capability-barrels` is a strict ancestor/subset: memory-capability-barrel = cli-capability-barrels + the memory-domain refactor on top, with the same dynamic-import fix commit already included at cli-capability-barrels' tip. Since memory-capability-barrel's full daemon suite run (above) already exercises this exact fix and its regression test, a second independent 430-file suite run against cli-capability-barrels would be redundant — the fix and its test coverage are the same code. Not re-run separately to conserve audit time; flagging this reasoning explicitly rather than silently skipping. |
| 3 | mcp-capability-barrel | #5135 | ✅ | Yes — `apps/daemon` vitest | Files: 417 passed / 9 failed / 4 skipped (430). Tests: 5451 passed / 25 failed / 7 skipped / 4 todo (5487) | No coverage script/config found for `apps/daemon` | Yes — merge-base == origin/main HEAD (0b88ef561), clean | 24 of 25 failures match the established baseline set exactly. One EXTRA failure not seen on any other branch: `tests/integrations/vela.test.ts > spawnVelaLogin > spawns login with the Settings-configured AMR profile over daemon env` — `Error: vela login already running`, thrown by `isVelaLoginInFlight()` guard in `src/integrations/vela.ts:814`. This reads as test-order/module-singleton-state leakage (an in-flight flag not reset before this test runs) rather than an mcp-domain regression — no mcp-specific test failed, and the error is about a login-in-progress guard, not mcp routing/protocol logic. Flagged rather than dismissed since it didn't reproduce on the 4 sibling daemon-suite runs already completed; Confirmed via isolated re-run: `vitest run tests/integrations/vela.test.ts` alone passes 31/31 — this is cross-file test-order flakiness in the full-suite run, not a bug in this branch. |
| 3 | automation-capability-barrel | #5130 | ✅ | Yes — `apps/daemon` vitest | Files: 418 passed / 8 failed / 4 skipped (430). Tests: 5418 passed / 24 failed / 7 skipped / 4 todo (5453) | No coverage script/config found for `apps/daemon` | Yes — merge-base == origin/main HEAD (0b88ef561), clean | Same established baseline failure set (amr-session-resume, connection-test, media/policy-routes, xai/tokens, run-failure-telemetry-smoke) — 4th independent branch reproducing it identically. No automation-domain-specific test failed. |
| 3 | agents-capability-barrel | #5148 | ✅ | Yes — `apps/daemon` vitest | Files: 416 passed / 8 failed / 4 skipped (428). Tests: 5415 passed / 23 failed / 7 skipped / 4 todo (5449) | No coverage script/config found for `apps/daemon` (same as every other daemon-only branch in this audit) | Yes — merge-base == origin/main HEAD (0b88ef561), clean | Same 23-test baseline failure set as codex-capability-barrel/memory-capability-barrel (amr-session-resume, connection-test, media/policy-routes, xai/tokens EACCES-as-root, run-failure-telemetry-smoke) — 3rd independent branch reproducing the identical environment-driven failures. No agents-domain-specific test failed. Reviewer's "approved but closed for organizational reasons" claim holds up: this is a clean, green (modulo baseline noise) refactor. |
| 2 | refactor/web-memory-slice | #5228 (closed) | ✅ | Yes — `apps/web` vitest (`vitest run -c vitest.config.ts --maxWorkers=2 --coverage`, `@vitest/coverage-v8` present) | Full suite: Files 447 passed / 2 failed (449). Tests 4831 passed / 6 failed / 7 skipped (4844). The 6 failures are in `App.connectors.test.tsx` (first-run banner telemetry flow) and `FileWorkspace.design-system.test.tsx` (archive-download event ordering) — unrelated to the memory slice; no `Memory*.test.tsx` file failed. | Full-suite run did not print a coverage table (coverage provider produced no console summary or `coverage/` dir on this run — possibly swallowed alongside the unrelated failures; not chased further). Re-ran coverage scoped to just the memory feature/slice files+tests instead (28 files, 436/436 tests passed): **Statements 96.16% (1454/1512), Branches 91.29% (933/1022), Functions 96.29% (338/351), Lines 96.92% (1261/1301)**. Per-file: `MemorySection.tsx` 98.3%/96.92%/96.42%/100%; `MemoryModelInline.tsx` (the file with the caller bug) only 65.45%/43.5%/65.71%/70.8% — lower because large branches of its BYOK-vs-CLI/custom-model UI logic aren't exercised, but the specific async-error-handling lines the fix touches ARE covered by `MemoryModelInline.test.tsx`'s two malformed-response tests (see note). | Yes — merge-base == origin/main HEAD (0b88ef561), trivial fast-forward, 0 conflicts possible | **This is the reference implementation's key question, and the answer is: the caller bug IS fixed on the branch's final head.** Tip commit `d695f1e0f` "fix(web): harden memory async state and response handling" is exactly the fix: `providers/memory/entries.ts`'s `fetchMemoryExtractionConfig()` now `throw`s on `!resp.ok` and routes the `extraction` field through `requiredField()` (throws if the field is absent from a malformed 2xx), and `MemoryModelInline.tsx`'s mount effect uses `.then(onSuccess, onError)` (not a bare `.then(onSuccess)`) so the throw is caught, plus its `persist()` write path wraps `patchMemoryExtractionConfig` in try/catch. The commit adds `apps/web/tests/components/MemoryModelInline.test.tsx` with two tests specifically named "shows a visible error when a user save receives a malformed successful response" and "converts a malformed debounced re-sync response into the same visible error" — both independently confirmed passing in this run. Jini's equivalent memory-slice pattern can safely copy this branch's final-head shape for the throw/catch contract between the provider and the component. Caveat: this branch is 271 commits / ~2100 files ahead of the fork's frozen `main` (huge accumulated unrelated history — skills, tools/release, tools/pack, etc.) — the diffstat size reflects that drift, not the size of the actual fix. |
