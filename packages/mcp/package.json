{
  "name": "@jini/mcp",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "repository": { "type": "git", "url": "https://github.com/leonaburime-ucla/Jini.git", "directory": "packages/mcp" },
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "bin": { "jini-mcp": "./dist/bin/serve.js" },
  "files": ["dist"],
  "publishConfig": { "access": "restricted", "registry": "https://registry.npmjs.org" },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/bin/serve.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@jini/cli": "workspace:*",
    "@jini/platform": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "undici": "^7.25.0"
  }
}
