// @jini/agent-runtime — see docs/jini-port/extraction-plan.md for the target
// shape; craft/ and skills/ are content-only (no barrel export — consumed by
// path), registry.ts is this package's first real TypeScript surface.
export * from './registry.js';
