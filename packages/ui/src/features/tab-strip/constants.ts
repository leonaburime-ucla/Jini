/** Ported verbatim from `WorkspaceTabsBar.tsx`'s `TAB_DRAG_HAPTIC_MS`. */
export const TAB_STRIP_DRAG_HAPTIC_MS = 8;

/** Ported verbatim from `WorkspaceTabsBar.tsx`'s `TAB_DROP_HAPTIC_MS`. */
export const TAB_STRIP_DROP_HAPTIC_MS = 12;

/** DOM attribute used for strip-level drop-target hit-testing (the generic
 *  form of `WorkspaceTabsBar.tsx`'s `data-workspace-tab-id`). */
export const TAB_STRIP_ITEM_ID_ATTRIBUTE = 'data-tab-strip-item-id';
