/**
 * Re-exported (2026-07-18) from the promoted, fully generic
 * `browser/useFileDropTarget` — see that file's doc comment for why. Kept
 * under this feature's original name/import path so nothing else in
 * `asset-tree-browser` (or its tests) has to change.
 */
export { useFileDropTarget as useAssetTreeDragUpload } from '../../../../browser/useFileDropTarget.js';
export type { UseFileDropTargetResult as UseAssetTreeDragUploadResult } from '../../../../browser/useFileDropTarget.js';
