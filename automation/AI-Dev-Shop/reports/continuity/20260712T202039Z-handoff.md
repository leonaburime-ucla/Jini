# Handoff — Slash-Command Protocol Hardening + Stale-Command Guard + External Audit

- **Created:** 2026-07-12T20:20:39Z
- **Target:** claude (Claude Code restart)
- **Branch:** `main` (HEAD `08e4531`) — ⚠️ working directly on main; nothing from this session is committed yet
- **Focus for next session:** decide how to commit this session's work (protocol edits + stale-command guard + `.claude/commands` re-sync), optionally re-dispatch the corrected audit packet, and handle the pre-existing unrelated worktree changes.

---

## 1. What this session did (all UNCOMMITTED)

### A. `/debate` rule changes — `framework/slash-commands/debate.md`
- **Debate Routing Guard** reworked: external peers mandatory *by default*; two explicit-request exceptions — **Replacement** (subagent instead of peers) and **Addition** (subagent *alongside* peers, additive-only, distinct from the same-family Primary-slot helper).
- **New Debate Round-Disclosure Guard**: Round 1 blind (no Coordinator opinion/findings/Primary answer in the packet; Primary still forms a frozen first-pass per consensus.md Step 8); Round 2+ informed (Coordinator shares discoveries/deltas).
- **Audit fix applied this session:** qualified the "mandatory" sentence to `"mandatory by default: absent an explicit user request to do otherwise (the Replacement exception below)…"` — resolves a real internal-contradiction flagged by Gemini.
- NOTE: the first debate.md changes were committed earlier (`6d11ac2`); the "mandatory by default" line is the only UNCOMMITTED debate.md edit.

### B. agy/Gemini file-staging fix + Solution Slate Protocol — `skills/llm-operations/references/peer-llm-dispatch.md`
- **agy fix:** replaced "run from /tmp" with "run from a dir with **no `AGENTS.md` in cwd or any ancestor**" and topology-aware staging base: subfolder install → `<ADS_MEMORY_ROOT>/tmp/peer-dispatch/<workflow>/`; single-folder install (where `<ADS_MEMORY_ROOT>` is INSIDE the repo tree, which is THIS repo) → external base `${TMPDIR:-/tmp}/ads-peer-dispatch/<workflow>/`. Reference ONLY staged paths, never original repo paths.
- **New "Solution Slate Protocol" section:** any fix/audit/issue must produce a ranked slate (≥2 options, explicit ranking criteria, per-option trade-offs + genuine sacrifice, recommendation + cheapest de-risking test). Command-specific artifact: `/debate` → sample code/pseudocode; `/audit-work` → diff-level fix per option → Disposition Gate; `/cowork` → ranked correction plans; `/consensus` single-pass → ranked slate. **Scope guard: NOT in /debate Round 1.**

### C. Solution Slate pointers wired into 3 commands (source of truth = framework/slash-commands)
- `consensus.md` — added mandatory Peer-Dispatch-Brief **file-context line** (tell user peers read repo files for better results) + Solution Slate pointer at Step 9.
- `cowork.md` — Solution Slate pointer at synthesis (ranked correction slate before applying).
- `audit-work.md` — Solution Slate pointer at step 11 (ranked remediation → Disposition Gate; governs OUTPUT only, preserves step-9c input exclusion).

### D. `.claude/commands/` comprehensive re-sync (the big worktree noise)
- Ran `install-slash-commands.sh --install --overwrite`: overwrote 8 stale installed commands (`agent, blueprint, clarify, code-review, implement, plan, spec, tasks`) from the comprehensive framework sources; installed 2 missing (`handoff`, `reverse-spec`). gstack-* project commands intentionally NOT installed (opt-in `--include-project`).
- Root cause of the staleness: framework sources had drifted ahead of installed copies (e.g. `plan.md` 80 vs 19 lines).

### E. New stale-command guard — `harness-engineering/validators/validate_slash_command_parity.py` (NEW FILE)
- Flags DIVERGED (installed copy differs from `framework/slash-commands` source) as VIOLATION exit 1 with FIX telemetry; MISSING / opt-in `gstack-*` / EXTRA are informational notes (never fail).
- Wired into `run-all.sh` **hard checks (line 24)** + **precommit (line 46)**; documented in `harness-engineering/validators/README.md`.
- Tested: passes clean; catches an injected stale edit (exit 1); restores.

---

## 2. Completed `/audit-work` (real external dispatch)

Full report: `ADS-memory/.local-artifacts/external-audit/runs/20260712T195746Z-external-audit-report.md`
Packet: `ADS-memory/.local-artifacts/external-audit/packets/20260712T195746Z-audit-packet.md`

- **Auditors:** internal Opus subagent (9/10 PASS) + **codex** GPT-5.5 xhigh (8/10 FAIL) + **agy/Gemini 3.1 Pro (High)** (6/10 FAIL). No degraded coverage.
- **Finding 1 (both peers): "parity guard crashes" = FALSE POSITIVE** on the real file — caused by MY packet mislabeling a condensed pseudocode excerpt as "Full source." Real file verified: imports `Path`, runs clean, catches staleness, wired. Surfaced as Decision Point (2-auditor convergence).
- **Finding 2 (Gemini): debate.md contradiction = REAL → FIXED** (see 1.A "mandatory by default"). Disagreed with Gemini's fix (delete Replacement) — Replacement is intended pre-existing user override.
- **Finding 3 (codex advisory): agy reference-only-staged-paths → APPLIED** (see 1.B).
- All 5 proposed fixes dispositioned (3 agree-implement ✓, 2 disagree w/ evidence). Audit Outcome: **PASS post-remediation** — but peers failed the packet-as-sent, largely due to my packet defect.

**Post-audit validators run:** `validate_debate_routing_guard.py` PASS, `validate_slash_command_parity.py` PASS.

---

## 3. Pre-existing UNRELATED worktree changes — DO NOT bundle without asking
These predate this session (CIC skill + roast skill + agent skills work) and are NOT part of the above:
`AGENTS.md`, `agents/*/skills.md` (7 files), `framework/routing/skills-registry.md`, `framework/templates/{README,tasks-template}.md`, `framework/workflows/multi-agent-pipeline.md`, `framework/templates/critical-internal-constraints-template.md` (untracked), `skills/critical-internal-constraints/` (untracked), `skills/roast/` (untracked).

---

## 4. Open decisions for next session
1. **Commit strategy** — nothing committed. On `main` (no feature branch). Suggest: branch, then commit this session's work as focused commits, e.g.
   - commit 1: `debate.md` mandatory-by-default fix + `peer-llm-dispatch.md` (agy staging fix + Solution Slate Protocol) + the 3 command pointers (consensus/cowork/audit-work).
   - commit 2: stale-command guard (`validate_slash_command_parity.py` + `run-all.sh` + validators `README.md`).
   - commit 3: `.claude/commands/` comprehensive re-sync (12 modified + `handoff.md` + `reverse-spec.md`).
   - Leave the section-3 pre-existing changes untouched (they're separate work).
2. **Re-dispatch corrected packet?** Optional — a clean cross-auditor PASS now that the debate.md contradiction is fixed and the packet excerpt issue is understood. Only value is a clean score; no real defect remains.
3. **Report retention** — audit report + handoff are in `.local-artifacts/` (local-only) by default; promote to `reports/` only if you want them retained.
4. **Advisory not yet done (internal-verifier path-to-10):** add a one-line direct Solution-Slate pointer in `debate.md` so `/debate` doesn't rely solely on delegation to `consensus.md`. Optional hardening.

## 5. Reusable gotchas surfaced (already saved to memory)
- **agy flag-order trap:** `--print` must come AFTER `--model`, else agy swallows `--model` as the prompt and silently falls back to **Claude Sonnet 4.6** (wrong family, no error). Correct: `agy --model "Gemini 3.1 Pro (High)" --print "<prompt>"`, TTY-wrapped via `script -q /dev/null`, run from a clean cwd. Saved in memory `agy-peer-dispatch-marker`.
- **codex 0.144.0** (>0.140 tool-use quarantine) works fine for self-contained no-tool reasoning via stdin: `codex exec --ignore-rules --ignore-user-config --ephemeral -s read-only -c features.multi_agent=false -c 'hooks.SessionStart=[]' -`.

---

## Next-Agent Prompt (paste to restart)
> Read `<AI_DEV_SHOP_ROOT>/AGENTS.md` first, then `ADS-memory/.local-artifacts/handoff/20260712T202039Z-handoff.md`. We're on branch `main` with uncommitted work from a slash-command protocol session: debate/consensus/cowork/audit-work.md edits, the agy staging fix + Solution Slate Protocol in peer-llm-dispatch.md, a new `validate_slash_command_parity.py` stale-command guard (wired into run-all.sh hard+precommit), and a full `.claude/commands/` re-sync. A `/audit-work` external audit (codex + agy/Gemini) already ran and PASSED post-remediation — report at `ADS-memory/.local-artifacts/external-audit/runs/20260712T195746Z-external-audit-report.md`. Do NOT touch the pre-existing unrelated worktree changes (AGENTS.md, agents/*/skills.md, framework/templates/*, skills/critical-internal-constraints, skills/roast). Start with Open Decision #1: propose a branch + focused-commit plan for this session's work only, and wait for my go before committing or pushing.
